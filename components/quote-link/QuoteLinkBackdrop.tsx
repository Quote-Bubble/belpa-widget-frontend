/**
 * Quote Link backdrop — bluish white with a soft blue glow at the bottom.
 */
export function QuoteLinkBackdrop() {
  return (
    <div className="quote-link__sky" aria-hidden>
      <div className="quote-link__glow" />
    </div>
  );
}
